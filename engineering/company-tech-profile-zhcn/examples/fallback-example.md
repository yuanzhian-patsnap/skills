# 降级示例

若结构化专利/论文检索不可用，本技能应按如下方式继续执行：

1. 在 `method_decisions.md` 中记录最高置信度的结构化数据源不可用。
2. 尽可能使用任何可用的学术数据库、官方文件或领域专项数据源。
3. 使用 `Exa` 进行广域公开网络发现。
4. 若可用，使用 `Tavily` 或 `Brave` 进行补充发现。
5. 使用通用 `web` 搜索作为兜底。
6. 使用 `Jina` 或其他已知 URL 阅读器，读取关键官方页面或 PDF。

最终报告应明确说明本次运行使用了降级证据路径。
