# 降级示例

若结构化专利/论文检索不可用，技能仍应按以下方式推进：

1. 在 `method_decisions.md` 中记录最高置信度的结构化来源不可用。
2. 尽可能使用任何可用的学术数据库、官方申报文件或领域专属来源。
3. 使用 `Exa` 进行广域公开网络发现。
4. 若可用，使用 `Tavily` 或 `Brave` 进行补充发现。
5. 使用通用 `web` 搜索作为降级手段。
6. 使用 `Jina` 或其他已知 URL 阅读器，读取关键的官方页面或 PDF。

最终报告应明确说明：玩家完整性和路线广度基于降级的证据路径。
