# 降级示例

若结构化专利/论文检索不可用，本技能仍应按以下方式继续执行：

1. 在 `method_decisions.md` 中记录最高置信度结构化来源不可用的情况。
2. 尽可能使用任何可用的学术数据库或领域专属来源。
3. 使用 `Exa` 进行宽泛的公开网络发现。
4. 若可用，使用 `Tavily` 或 `Brave` 进行补充发现。
5. 使用通用 `web` 搜索作为降级手段。
6. 使用 `Jina` 或其他已知 URL 阅读器，阅读关键官方页面或 PDF。

最终报告应明确说明：路线广度和前沿完整性是在降级证据路径下产出的。
